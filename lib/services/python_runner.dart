import 'package:flython/flython.dart';

class PythonRunner extends Flython {
  runCode(String code) {
    // ignore: unused_local_variable
    Flython runner = Flython();

    // runner.initialize("python", mainApp, debugMode);
  }
}
