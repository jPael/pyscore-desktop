class Posts {
  final String id;
  final String title;
  final String instructions;
  final String points;
  final String due;

  Posts(this.id, this.title, this.instructions, this.points, this.due);
}
